import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

/**
 * Tailwind CSS 클래스를 병합하는 유틸리티 함수
 *
 * 📌 이 함수가 필요한 이유:
 * Tailwind에서 "bg-red-500 bg-blue-500" 같이 충돌하는 클래스가 있을 때,
 * 단순 문자열 연결로는 둘 다 적용되어 예상치 못한 결과가 나올 수 있어요.
 * cn() 함수는 충돌을 해결하고 마지막 값만 적용합니다.
 *
 * @example
 * cn("bg-red-500", "bg-blue-500") // "bg-blue-500" (마지막 값 적용)
 * cn("p-4", condition && "p-8")   // 조건에 따라 p-4 또는 p-8
 */
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}

/**
 * 랜덤 귀여운 이름 생성 함수
 *
 * 📌 사용 예시:
 * - 회원가입 시 자동으로 부여되는 닉네임
 * - 익명 댓글 작성자 이름
 *
 * @returns "행복한 고슴도치" 같은 형식의 랜덤 이름
 */
const adjectives = [
  "행복한",
  "귀여운",
  "씩씩한",
  "똑똑한",
  "용감한",
  "따뜻한",
  "밝은",
  "재빠른",
  "부지런한",
  "착한",
  "멋진",
  "예쁜",
  "신나는",
  "활발한",
  "든든한",
  "상냥한",
  "재미있는",
  "영리한",
  "사랑스러운",
  "빛나는",
]

const animals = [
  "고슴도치",
  "펭귄",
  "다람쥐",
  "토끼",
  "고양이",
  "강아지",
  "판다",
  "코알라",
  "햄스터",
  "수달",
  "여우",
  "사슴",
  "돌고래",
  "부엉이",
  "호랑이",
  "곰",
  "물개",
  "치타",
  "알파카",
  "카피바라",
]

export function generateRandomName(): string {
  // 배열에서 랜덤하게 하나씩 선택
  const randomAdjective = adjectives[Math.floor(Math.random() * adjectives.length)]
  const randomAnimal = animals[Math.floor(Math.random() * animals.length)]
  return `${randomAdjective} ${randomAnimal}`
}
